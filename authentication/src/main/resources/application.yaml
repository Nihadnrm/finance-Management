spring:
  application:
    name: authentication
  datasource:
    url: jdbc:mysql://localhost:3306/finance_Management
    username: root
    password: root123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET}